========================================
VSCode 前端调试完整指南
========================================

【VSCode调试简介】

VSCode内置了强大的调试功能，比浏览器DevTools更方便，因为：
- 代码和调试面板在同一窗口
- 可以直接在源代码设置断点
- 支持Vue、JavaScript等语言
- 可以在编辑器中查看变量值

========================================

【安装必需插件】

1. Debugger for Chrome 或 Debugger for Firefox
   - 发布者: Microsoft
   - 功能: 在VSCode中调试浏览器中的代码

2. Vetur (如果用Vue2) 或 Volar (如果用Vue3)
   - 发布者: Volar
   - 功能: Vue3语法支持和调试

3. ES7+ React/Redux/React-Native snippets
   - 发布者: dsznajder
   - 功能: JavaScript代码片段和支持

安装方法:
打开VSCode -> Extensions -> 搜索插件名 -> Install

========================================

【创建VSCode调试配置】

## 第一步: 打开调试配置文件

1. 打开VSCode
2. 按 Ctrl + Shift + D 打开运行和调试
3. 点击 "创建launch.json文件" 或 "创建配置"
4. 选择 "Chrome" 或 "Firefox"

## 第二步: 复制以下配置

在项目根目录创建 .vscode/launch.json:

{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Admin Frontend (3001)",
      "type": "chrome",
      "request": "launch",
      "url": "http://192.168.0.128:3001",
      "webRoot": "${workspaceFolder}/frontend/admin/src",
      "sourceMapPathOverride": {
        "webpack:///*": "${webRoot}/*"
      },
      "runtimeExecutable": "C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe",
      "runtimeArgs": ["--remote-debugging-port=9222"],
      "port": 9222
    },
    {
      "name": "H5 Frontend (3000)",
      "type": "chrome",
      "request": "launch",
      "url": "http://192.168.0.128:3000",
      "webRoot": "${workspaceFolder}/frontend/h5/src",
      "sourceMapPathOverride": {
        "webpack:///*": "${webRoot}/*"
      },
      "runtimeExecutable": "C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe",
      "runtimeArgs": ["--remote-debugging-port=9222"],
      "port": 9222
    }
  ]
}

========================================

【启动VSCode调试】

## 方法1: 使用Chrome调试（推荐）

1. 确保前端已启动（npm run dev 或 npx vite）
2. VSCode中按 Ctrl + Shift + D 打开运行和调试面板
3. 在下拉菜单选择 "Admin Frontend (3001)" 或 "H5 Frontend (3000)"
4. 按 F5 启动调试
5. 会自动打开Chrome浏览器，连接到VSCode进行调试

## 方法2: 手动连接浏览器

1. Chrome已经打开你的前端
2. VSCode -> 运行和调试 -> 点击 "Attach to Chrome"
3. VSCode会连接到Chrome进行调试

========================================

【断点调试】

## 设置断点

1. 在源代码左侧行号处点击，设置断点（会显示红点）
2. 刷新页面或执行操作
3. 代码执行到断点时会暂停

示例: 在登录方法设置断点

// src/views/Login.vue
const handleLogin = async () => {
  const form = {
    username: 'admin',
    password: 'admin123'
  }
  
  console.log('开始登录')
  // 在下一行设置断点（点击行号）
  const res = await login(form)  // <- 点击这一行左边设置断点
  
  console.log('登录响应:', res)
}

========================================

【调试面板功能】

当代码暂停在断点时，左侧会显示调试面板：

## 1. 变量面板 (Variables)
- Local（局部变量）
- Global（全局变量）
- 显示当前作用域的所有变量值

## 2. 监视 (Watch)
添加要监视的变量，每次暂停时都会显示最新值

使用: 右键变量 -> Add to Watch

## 3. 调用堆栈 (Call Stack)
显示代码执行的调用链

点击不同的堆栈帧，看对应位置的变量值

## 4. 断点 (Breakpoints)
显示所有已设置的断点

可以在这里：
- 禁用断点（取消勾选）
- 删除断点（右键）
- 跳过某些断点

========================================

【调试控制按钮】

调试时顶部会出现工具栏：

[ ⏸ ] 暂停 - 暂停执行
[ ► ] 继续 - 继续执行到下一个断点
[ → ] 单步执行 - 执行一行代码
[ ↓ ] 单步进入 - 进入函数内部
[ ↑ ] 单步退出 - 退出当前函数
[ 🔁 ] 重启 - 重新启动调试
[ ⏹ ] 停止 - 停止调试

快捷键：
- F10: 单步执行
- F11: 单步进入
- Shift + F11: 单步退出
- F5 或 Ctrl + F5: 继续/重启
- Shift + F5: 停止

========================================

【实战调试示例】

## 场景: 调试登录功能

步骤1: 打开Login.vue，在登录方法设置断点

// src/views/Login.vue
const handleLogin = async () => {
  const res = await login(form.value)  // <- 在这行设置断点
  
  if (res.code === 0) {
    localStorage.setItem('admin_token', res.data.token)
    router.push('/')
  }
}

步骤2: 启动调试
- Ctrl + Shift + D
- 选择 Admin Frontend (3001)
- F5 启动

步骤3: 在浏览器中操作
- 打开http://192.168.0.128:3001
- 输入用户名密码
- 点击登录

步骤4: VSCode会暂停在断点
- 看左侧Variables面板
- form.value显示你输入的数据
- 可以看res等待API响应

步骤5: 使用控制按钮调试
- 按F10单步执行
- 看res变量是否有返回值
- 看code是否为0

========================================

## 场景: 调试API请求

步骤1: 打开admin.js，在API调用处设置断点

// src/api/admin.js
export function getCardList(params) {
  console.log('请求参数:', params)
  return request({  // <- 在这行设置断点
    url: '/api/v1/admin/cards',
    method: 'get',
    params
  })
}

步骤2: 启动调试后操作卡片页面
- 在浏览器打开卡片列表页
- 代码会暂停在断点

步骤3: 检查变量
- params变量: 查看请求参数是否正确
- request函数的返回值: 查看是否返回Promise

步骤4: 单步进入request函数
- F11 进入request()内部
- 看axios是否正确配置

========================================

## 场景: 调试Vue组件数据

步骤1: 在Dashboard.vue的loadStats方法设置断点

// src/views/Dashboard.vue
const loadStats = async () => {
  const data = await getStatistics()  // <- 设置断点
  console.log('API返回:', data)
  stats.value = data  // <- 也可以在这行设置断点
}

步骤2: 启动调试
- 页面加载时会执行onMounted -> loadStats
- 代码暂停在断点

步骤3: 检查变量
- data: API返回的统计数据
- stats.value: 赋值前后的数据

步骤4: 使用Watch
- 点击Watch +
- 输入 stats.value
- 可以看到stats的所有属性

========================================

【条件断点】

普通断点每次都会暂停，条件断点只在满足条件时暂停

## 设置条件断点

右键断点（红点）-> 编辑断点 -> 输入条件

示例: 只在pageSize > 50时暂停

在分页方法设置条件断点:

const loadCards = async (page, pageSize) => {
  const result = await getCardList({ page, pageSize })  // <- 右键此处设置断点
  // 编辑条件: pageSize > 50
}

现在只有请求50条以上数据时才会暂停

## 日志点 (Logpoint)

右键断点 -> 添加日志点 -> 输入日志表达式

示例: 记录API请求数据

const handleSearch = (keyword) => {
  // 右键此处设置日志点
  // 表达式: 'Search for:' + keyword
  const results = search(keyword)
}

代码会继续执行，但会在控制台打印日志

========================================

【VSCode集成终端调试】

在VSCode中同时看代码、调试面板和终端输出

## 打开集成终端

- Ctrl + ` 打开集成终端
- 或 菜单 -> 终端 -> 新建终端

## 在终端启动前端

在VSCode集成终端中运行:

cd frontend/admin
npx vite --host 0.0.0.0 --port 3001

好处:
- 代码、调试、终端都在VSCode中
- 可以同时看前端输出和调试信息
- F5启动调试会自动连接

========================================

【快速调试技巧】

## 技巧1: 在console中执行表达式

调试暂停时，在底部调试控制台输入：

// 获取变量值
stats.value

// 修改变量值
stats.value.total_cards = 100

// 执行函数
loadStats()

// 访问localStorage
localStorage.getItem('admin_token')

## 技巧2: 使用debugger语句

在代码中直接写debugger，启动调试时会自动暂停：

// src/views/Dashboard.vue
const loadStats = async () => {
  debugger  // 运行到这里会自动暂停
  const data = await getStatistics()
  stats.value = data
}

## 技巧3: 快速重新启动调试

F5 快速键重新启动调试会：
- 刷新浏览器
- 清除所有断点信息
- 重新开始调试

## 技巧4: 使用代码片段和Watch

Watch可以监视复杂表达式：

// Watch中输入
stats.value.total_cards > 10 ? 'Many' : 'Few'

// 每次暂停时会计算这个表达式

========================================

【调试Vue组件特殊技巧】

## 查看Vue组件实例

在变量面板中展开对象，可以看到：
- data() 返回的数据
- props
- methods
- computed

## 调试生命周期

在不同生命周期钩子设置断点：

// src/views/Dashboard.vue
export default {
  setup() {
    const stats = ref({})
    
    onMounted(() => {
      debugger  // 组件挂载时暂停
      loadStats()
    })
    
    onUpdated(() => {
      debugger  // 组件更新时暂停
    })
    
    return { stats }
  }
}

## 调试响应式数据

在Watch中输入:

isRef(stats)  // 检查是否是响应式数据
stats.value  // 查看响应式数据的值
unref(stats)  // 获取未包装的值

========================================

【后端Node.js调试】

如果后端用Node.js, 也可以直接调试

## 配置Node.js调试

在 .vscode/launch.json 添加:

{
  "name": "Node Backend",
  "type": "node",
  "request": "launch",
  "program": "${workspaceFolder}/server.js",
  "restart": true,
  "console": "integratedTerminal"
}

然后按F5启动调试，可以在VS中调试Node.js代码

========================================

【调试快捷键总结】

F5             - 启动/继续调试
Shift + F5     - 停止调试
Ctrl + Shift + D - 打开运行和调试面板
F10            - 单步执行（不进入函数）
F11            - 单步进入（进入函数）
Shift + F11    - 单步退出（退出函数）
Ctrl + K Ctrl + I - 显示鼠标所在变量的值

========================================

【常见调试问题】

## 问题1: 调试无法启动，提示"chrome.exe not found"

解决:
编辑launch.json，改为你的Chrome路径：

Windows:
"runtimeExecutable": "C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe"

或使用默认值（留空）：
不配置runtimeExecutable，让VSCode自动找Chrome

## 问题2: 断点设置后不暂停

原因: 
- Chrome和VSCode连接失败
- Source Map配置错误
- 代码没有执行到断点

解决:
1. 检查控制台是否显示"Debugger attached"
2. 检查webRoot是否正确指向源代码目录
3. 刷新浏览器重新运行代码

## 问题3: 看不到源代码

原因: Source Map未正确配置

解决: 检查launch.json中的sourceMapPathOverride

## 问题4: 调试太慢

原因: 设置了太多断点或Watch表达式过于复杂

解决:
- 删除不必要的断点
- 简化Watch表达式
- 只监视必要的变量

========================================

【调试工作流程建议】

1. 打开项目文件夹
2. 在VSCode终端启动前端: npx vite --host 0.0.0.0 --port 3001
3. Ctrl + Shift + D 打开调试面板
4. 选择相应的调试配置
5. F5 启动调试
6. 浏览器自动打开
7. 在代码中设置断点
8. 浏览器中操作，触发代码执行
9. VSCode暂停在断点，查看变量面板
10. 使用F10/F11调试逐行代码
11. 完成后 Shift + F5 停止调试

========================================

【VSCode调试 vs 浏览器DevTools】

VSCode调试优势：
✓ 代码和调试面板在同一窗口
✓ 可以直接编辑代码（调试中无效，但可以准备修改）
✓ Watch表达式更强大
✓ 集成终端看输出

浏览器DevTools优势：
✓ 查看实时DOM和样式
✓ 更易于性能分析
✓ 实时修改CSS和HTML
✓ 不需要额外配置

最佳实践: 两者结合
- 用VSCode调试逻辑
- 用DevTools调试界面和性能

========================================

【推荐调试组合】

开发时建议：

1. VSCode左侧打开文件树
2. VSCode下方打开集成终端（运行前端）
3. VSCode右侧查看当前文件
4. F12打开Chrome DevTools（看实时界面）
5. Ctrl + Shift + D启动VSCode调试（调试逻辑）

这样可以同时看到：
- 源代码
- 前端输出日志
- 实时网页效果
- 变量监视和调试信息

========================================

【快速开始】

最快的方式启动调试：

1. 打开项目 (Ctrl + K Ctrl + O 或 文件 -> 打开文件夹)
2. Ctrl + ` 打开终端
3. 输入: cd frontend/admin && npx vite --host 0.0.0.0 --port 3001
4. Ctrl + Shift + D
5. 选择 Admin Frontend (3001)
6. F5
7. 浏览器打开，VSCode已连接
8. 在代码行号左侧点击设置断点
9. 浏览器操作触发代码
10. VSCode暂停并显示变量

========================================
